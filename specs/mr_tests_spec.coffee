mr_tests = require '../lib/mr_tests'

describe 'Mr. Tests', ->
  it 'should test a file', ->
    # uhh... i don't know what to do now
    # i guess i should give it a file name
    # and it would read the file in and assert
    # that the outcome matched the expectation.
    # I know how to do that.  What I don't know 
    # how to do is test that. Its too meta for
    # me right now.
